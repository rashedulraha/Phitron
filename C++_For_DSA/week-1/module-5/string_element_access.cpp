#include <bits/stdc++.h>
using namespace std;

int main() {
  string s;
  cin >> s;
  // cout << s[1] << endl;

  // print using method
  // cout << s.at(0) << endl;

  // print front and bank

  // cout << s.front() << endl;
  cout << s.back() << endl;        // same as
  cout << s[s.size() - 1] << endl; // same ass

  return 0;
}
